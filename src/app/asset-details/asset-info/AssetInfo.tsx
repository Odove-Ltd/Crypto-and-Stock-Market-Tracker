import React, {useEffect, useState} from 'react';

interface Props {
    // Define props here
}

const ComponentName: React.FC<Props> = (props) => {
    // Component logic here

    return (
        <div>Asset Info Container</div>
    );
};

export default ComponentName;
